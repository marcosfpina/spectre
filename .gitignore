# Rust
/target/
**/*.rs.bk
*.pdb
# Cargo.lock is committed for applications (binaries)
# Only libraries should ignore Cargo.lock

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Nix
result
result-*

# Logs
*.log
/tmp/

# Test artifacts
*.profraw
*.profdata

# SBOM (generated in CI)
*.cdx.json

# Environment
.env
.env.local

# Domain projects (separate repositories in ~/dev/low-level/)
# These should NOT be committed to SPECTRE repo
ai-agent-os/
arch-analyzer/
cognitive-vault/
intelagent/
ml-offload-api/
ragtex/
securellm-bridge/

# Empty directories removed during cleanup
docs/
# grafana/ — provisioning configs are tracked (removed by M4)
python/

# Nix modules are now tracked (contain K8s orchestration code)
